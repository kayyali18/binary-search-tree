export default class Node {
  constructor(left, right, value) {
    // Initialize your Node here!
    this.left = left; //points to left child node
    this.right = right; //points to right child node
    this.value = value //value of current Node
  }

} 